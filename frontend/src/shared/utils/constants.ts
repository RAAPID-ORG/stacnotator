/**
 * Application-wide constants
 */

// Keyboard shortcuts
export const DIGIT_INPUT_TIMEOUT_MS = 300;

// Default zoom level
export const DEFAULT_MAP_ZOOM = 13;

// Window a time series lands in when it declares no window_name of its own.
export const DEFAULT_TIMESERIES_WINDOW_NAME = 'Time series';
