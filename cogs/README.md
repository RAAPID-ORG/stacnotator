# Local COGs served from disk by the optional tiler (COMPOSE_PROFILES=tiler).
# Drop *.tif here and register a custom map with cog_url: /data/cogs/<file>.tif
# The tiler mounts this dir read-only at /data/cogs. The .tif files are gitignored.
